#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float radio, area;
    const float PI = 3.1416;
    cout << "Ingrese el radio del círculo: ";
    cin >> radio;
    area = PI * pow(radio, 2);
    cout << "El área del círculo es: " << area << endl;
    return 0;
}
